package com.cuupe.shinkou.common.enums;

/**
 * 枚举说明
 * | status     | 说明   |
 * | ---------- | ------ |
 * | `PENDING`  | 待接受 |
 * | `ACCEPTED` | 已接受 |
 * | `EXPIRED`  | 已过期 |
 * | `REVOKED`  | 已撤销 |
 */
public enum InvitationStatu {
    PENDING,
    ACCEPTED,
    EXPIRED,
    REVOKED
}
