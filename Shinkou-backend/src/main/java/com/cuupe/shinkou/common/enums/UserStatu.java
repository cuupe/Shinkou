package com.cuupe.shinkou.common.enums;

/**
 * 枚举说明
 * | status     | 说明   |
 * | ---------- | ------ |
 * | `PENDING`  | 待激活 |
 * | `ACTIVE`   | 正常   |
 * | `DISABLED` | 已禁用 |
 * | `LOCKED`   | 已锁定 |
 */
public enum UserStatu {
    PENDING,
    ACTIVE,
    DISABLED,
    LOCKED
}
